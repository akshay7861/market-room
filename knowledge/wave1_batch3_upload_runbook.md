# Wave 1 Batch 3 — Upload Runbook

**Date:** 2026-04-10
**Scope:** Upload and approval confirmation for 3 files only
**Prerequisite:** All Batch 2 gate conditions passed (confirmed 2026-04-10)
**Admin API base:** `http://127.0.0.1:8787` (local dev) or your deployed Worker URL

---

## 1. Diagnosis

**Status: Uploaded, approved, and storage-verified. All three files are live.**

Batch 3 completes the Wave 1 knowledge library. These are the three analytical framework docs — all depend on the foundations and playbooks from Batches 1 and 2 being active first. No code changes were needed. The pipeline behaved identically to Batches 1 and 2.

**Why these are the right Batch 3 docs — and why they are last:**

| File | Why last |
|------|---------|
| `term-premium-breakeven-interpretation-guide.md` | References the ACM decomposition and curve regimes introduced in `yield-curve-mechanics-and-interpretation.md` (Batch 2). Low standalone utility without the curve foundation active — the decomposition is only meaningful once the agent can classify curve regimes. Triggered by Treasury auctions and fiscal stress events, which are less frequent than CPI or EIA weekly. |
| `central-bank-reaction-function-framework.md` | Synthesises both inflation data (Batch 1 `inflation-transmission-mechanisms.md`) and labour market data (Batch 2 `labor-market-deterioration-playbook.md`) into Fed policy logic. Most powerful after both input docs are active. Triggered by FOMC weeks — 8 per year, plus minutes and Beige Book. |
| `commodity-curve-shape-and-physical-tightness-guide.md` | Adds futures curve structure on top of the EIA inventory foundation (Batch 1). The curve shape is only interpretable if the agent already knows what inventory thresholds mean. Triggered post-EIA Wednesdays and OPEC meeting weeks — overlaps with both Batch 1 and Batch 2 Commodities docs. |

---

## 2. Batch 3 Upload List

| # | File (full path from repo root) | Agent | Admin category | Agent ID | Market cases later |
|---|--------------------------------|-------|----------------|----------|--------------------|
| 1 | `knowledge/rates/frameworks/term-premium-breakeven-interpretation-guide.md` | Rates | **`frameworks`** | `rates-agent` | No |
| 2 | `knowledge/macro/frameworks/central-bank-reaction-function-framework.md` | Macro | **`frameworks`** | `macro-agent` | Yes |
| 3 | `knowledge/commodities/frameworks/commodity-curve-shape-and-physical-tightness-guide.md` | Commodities | **`frameworks`** | `commodities-agent` | Yes |

**Market cases note for File 1:** The term premium / breakeven guide is an analytical reference — it prevents misattribution errors (term premium shock misread as inflation expectations; TIPS liquidity squeeze misread as deflation). No standalone market cases are needed; its value is in sharpening the quality of posts generated by other catalysts.

---

## 3. Category Mapping

All three Batch 3 docs are frameworks. YAML `doc_type: framework` → Admin UI: **`frameworks`**.

| YAML `doc_type` | Select in admin UI |
|-----------------|-------------------|
| `framework` | `frameworks` |

---

## 4. Admin Upload Steps

All three uploaded in a single pass on 2026-04-10. Steps documented below for reference or re-upload.

---

### File 1 — `term-premium-breakeven-interpretation-guide.md` → Rates → `frameworks`

**Full path:** `knowledge/rates/frameworks/term-premium-breakeven-interpretation-guide.md`

**Via admin UI:** Open Rates Agent → AgentKnowledgeManager → select `frameworks` → upload file.

**Via curl:**
```bash
curl -s -X POST "http://127.0.0.1:8787/api/admin/agents/rates-agent/knowledge-store" \
  -F "category=frameworks" \
  -F "files=@knowledge/rates/frameworks/term-premium-breakeven-interpretation-guide.md;type=text/markdown"
```

**What gets stored:**
- Title: `"Term Premium and Breakeven Interpretation Guide"`
- Summary: `"frameworks covering: term premium, ACM model, TIPS breakevens, real yields."`
- `## Coverage`: term premium, ACM model, TIPS breakevens, real yields, inflation expectations, 5y5y forward, inflation risk premium, duration risk, fiscal supply, QE compression
- `## Triggers`: ACM term premium rising >50 bps in 6 weeks; 10-year real yield crossing 0%; 5y5y breakeven above 2.5%; 5y5y breakeven above 2.7% for more than 10 trading days; TIPS breakeven collapsing during equity stress; nominal yield rising while real yield is flat
- `review_status`: `approved` immediately
- `category`: `frameworks`

---

### File 2 — `central-bank-reaction-function-framework.md` → Macro → `frameworks`

**Full path:** `knowledge/macro/frameworks/central-bank-reaction-function-framework.md`

**Via admin UI:** Open Macro Agent → AgentKnowledgeManager → select `frameworks` → upload file.

**Via curl:**
```bash
curl -s -X POST "http://127.0.0.1:8787/api/admin/agents/macro-agent/knowledge-store" \
  -F "category=frameworks" \
  -F "files=@knowledge/macro/frameworks/central-bank-reaction-function-framework.md;type=text/markdown"
```

**What gets stored:**
- Title: `"Central Bank Reaction Function Framework"`
- Summary: `"frameworks covering: FOMC, dot plot, dual mandate, terminal rate."`
- `## Coverage`: FOMC, dot plot, dual mandate, terminal rate, r-star, neutral rate, average inflation targeting, forward guidance, FOMC statement language, Beige Book, Fed pivot
- `## Triggers`: FOMC meeting day; dot plot (SEP) release; FOMC statement language change; Fed Chair press conference; FOMC minutes release; Fed Governor speech by voting member; Beige Book two consecutive softening characterisations
- `review_status`: `approved` immediately
- `category`: `frameworks`

**Market cases to generate (after upload confirmed):**
1. Dot plot median shift >25 bps — agent posts policy thesis citing the shift magnitude and which meetings repriced
2. "Skip" vs "pause" language at an FOMC meeting — agent uses the Fed's exact word and explains the implication
3. Mid-cycle adjustment framing (75–100 bps max) vs full pivot — agent avoids calling it a new easing cycle prematurely

---

### File 3 — `commodity-curve-shape-and-physical-tightness-guide.md` → Commodities → `frameworks`

**Full path:** `knowledge/commodities/frameworks/commodity-curve-shape-and-physical-tightness-guide.md`

**Via admin UI:** Open Commodities Agent → AgentKnowledgeManager → select `frameworks` → upload file.

**Via curl:**
```bash
curl -s -X POST "http://127.0.0.1:8787/api/admin/agents/commodities-agent/knowledge-store" \
  -F "category=frameworks" \
  -F "files=@knowledge/commodities/frameworks/commodity-curve-shape-and-physical-tightness-guide.md;type=text/markdown"
```

**What gets stored:**
- Title: `"Commodity Curve Shape and Physical Tightness Guide"`
- Summary: `"frameworks covering: futures curve, contango, backwardation, calendar spread."`
- `## Coverage`: futures curve, contango, backwardation, calendar spread, M1-M2 spread, roll yield, physical tightness, WTI forward curve, curve kink, speculative positioning, storage economics, CFTC commitment of traders
- `## Triggers`: M1-M2 WTI spread crossing ±$1.00/bbl; M1-M13 WTI spread crossing ±$3.00/bbl; M1-M13 spread moving >$2 in 2 weeks; contango flattening from -$5 to -$2 over 3 weeks; curve kink appearing; speculative net longs at 5-year high combined with curve still in contango
- `review_status`: `approved` immediately
- `category`: `frameworks`

**Market cases to generate (after upload confirmed):**
1. M1–M13 WTI spread crossing +$3/bbl with EIA inventory confirms — agent posts structural tightness thesis
2. Contango flattening from -$6 to -$2 over 4 weeks — agent frames as leading indicator of price floor, not a bullish thesis yet
3. CFTC speculative net longs at 5-year high with only shallow backwardation — agent flags fragility, stays silent on new bullish thesis

---

## 5. Storage Verification — Confirmed 2026-04-10

All checks run immediately after upload. Results:

| Doc | `reviewStatus` | Title stored | YAML stripped | `## Coverage` | `## Triggers` | `usageBytes` |
|-----|---------------|--------------|---------------|---------------|---------------|--------------|
| `term-premium-breakeven-interpretation-guide.md` | **approved** | "Term Premium and Breakeven Interpretation Guide" | ✓ | ✓ | ✓ | 16,808 |
| `central-bank-reaction-function-framework.md` | **approved** | "Central Bank Reaction Function Framework" | ✓ | ✓ | ✓ | 16,014 |
| `commodity-curve-shape-and-physical-tightness-guide.md` | **approved** | "Commodity Curve Shape and Physical Tightness Guide" | ✓ | ✓ | ✓ | 21,863 |

---

## 6. Approval Confirmation Checklist (Re-verification Commands)

If you need to re-verify at any point:

```bash
# Term premium — Rates
curl -s "http://127.0.0.1:8787/api/admin/agents/rates-agent/knowledge-processing/jobs" | \
  python3 -c "
import json,sys; data=json.load(sys.stdin)
for job in data.get('jobs',[]):
    for item in job.get('items',[]):
        if 'term-premium' in item.get('sourceFilename',''):
            md=item.get('distilledMarkdown','') or ''
            print('reviewStatus:', item['reviewStatus'], '| title:', item['title'])
            print('has Coverage:', '## Coverage' in md, '| has Triggers:', '## Triggers' in md, '| YAML:', md.startswith('---'))
"

# Central bank — Macro
curl -s "http://127.0.0.1:8787/api/admin/agents/macro-agent/knowledge-processing/jobs" | \
  python3 -c "
import json,sys; data=json.load(sys.stdin)
for job in data.get('jobs',[]):
    for item in job.get('items',[]):
        if 'central-bank' in item.get('sourceFilename',''):
            md=item.get('distilledMarkdown','') or ''
            print('reviewStatus:', item['reviewStatus'], '| title:', item['title'])
            print('has Coverage:', '## Coverage' in md, '| has Triggers:', '## Triggers' in md, '| YAML:', md.startswith('---'))
"

# Commodity curve — Commodities
curl -s "http://127.0.0.1:8787/api/admin/agents/commodities-agent/knowledge-processing/jobs" | \
  python3 -c "
import json,sys; data=json.load(sys.stdin)
for job in data.get('jobs',[]):
    for item in job.get('items',[]):
        if 'commodity-curve' in item.get('sourceFilename',''):
            md=item.get('distilledMarkdown','') or ''
            print('reviewStatus:', item['reviewStatus'], '| title:', item['title'])
            print('has Coverage:', '## Coverage' in md, '| has Triggers:', '## Triggers' in md, '| YAML:', md.startswith('---'))
"
```

---

## 7. Complete Wave 1 Doc Count Per Agent

After Batch 3, each agent's approved Wave 1 doc pool:

| Agent | Approved Wave 1 Docs | Titles |
|-------|----------------------|--------|
| **Rates** | 3 | Fed Repricing Playbook · Yield Curve Mechanics and Interpretation · Term Premium and Breakeven Interpretation Guide |
| **Macro** | 3 | Inflation Transmission Mechanisms · Labor Market Deterioration Playbook · Central Bank Reaction Function Framework |
| **Commodities** | 3 | Oil Supply-Demand and Inventory Framework · OPEC and Geopolitical Shock Playbook · Commodity Curve Shape and Physical Tightness Guide |

Each agent now has one foundation/framework anchoring its reasoning, one event-playbook for its highest-frequency catalyst, and one analytical framework for its most common misattribution error.
